import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.snartbasecontacts',
  appName: 'Empact Card',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
